import React from 'react'

interface CustomersDepositLayoutProps {
  children: React.ReactNode
}

export default function CustomersDepositLayout({
  children,
}: CustomersDepositLayoutProps) {
  return <>{children}</>
}
