import React from 'react'

interface CustomersPaymentsLayoutProps {
  children: React.ReactNode
}

export default function CustomersPaymentsLayout({
  children,
}: CustomersPaymentsLayoutProps) {
  return <>{children}</>
}
